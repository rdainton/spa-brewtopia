describe('useDecklistStore', () => {
  // inits from localstorage
  // future changes to decklist are persisted to localstorage, including unsavedChanges state
  // resetting state also clears localstorage
  // get, store, and update hits api, returns promise
  // saveChanges will conditional create/update a resource

  // NOTE: the majority of decklist manipulations are done
  // through other hooks, that utilise the persistToLocalStorage method exposed here

  test('TODO', () => {
    return
  })
})
