import { mount } from '@vue/test-utils'
import BrewButton from '@/components/molecules/BrewButton.vue'

test('renders a button', () => {
  // Arrange
  const wrapper = mount(BrewButton)

  // Act

  // // 'Assert
  expect(true).toBe(true)
})
